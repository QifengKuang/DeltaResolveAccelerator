# 更新记录

## 1.1.1 — 2026-09-25

将本机已使用的 1.1.1 版本整理为完整源码与配套程序包，整合此前尚未进入主分支的会话恢复修复。

- 普通关闭、异常退出恢复和跨重启恢复按当前加速资源判定，不再要求整台电脑恢复到启动前快照。Tailscale、WSL、网卡、DHCP 和 IPv6 的外部变化保留为诊断。
- 重启后先核对开机边界与进程创建时间，避免使用已被复用的旧进程编号。新连接重新采集基线；读取暂态失败有界重试。
- 控制入口和 worker 在启动前检查残留。实际 SDK 进程、专属网卡、路由、DNS 或代理残留及不完整读取仍会阻止连接；旧诊断快照缺失不会永久锁住已释放的会话。
- 完整包统一构建界面、启动器、配置、图标和 12 个后端脚本，附源码提交、编译器及文件 SHA-256 清单。第三方运行时、SDK、密钥、设置和现场记录不随包发布。

2026-09-19 在本机完成两轮真实开启、线路探测、关闭、再次开启，均成功；历史故障快照重放通过。未人为强制重启电脑，重启场景由离线回归覆盖。2026-09-25 整理时，已安装的 12 个后端和 29 个原始构建输入均与对应源码／安装清单一致；源码目录里的旧 1.1.0 EXE 已重新构建。

本版未改变云端线路模式或试用截止日期。旧签名自动更新通道仍只分发界面文件；本版完整后端修复通过独立完整包交付，不能仅运行旧轻量安装器或检查界面更新来取得。
